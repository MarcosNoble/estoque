{{#each produtos}}
<div class="card mt-3">
    <div class="card-body">        
        <h3>{{nome}}</h3>
        <p>{{descricao}}</p><br>
        <p>{{peso}}</p><br>        
    </div>
</div>
{{else}}
    <h5>Nenhum produto</h5>
{{/each}}